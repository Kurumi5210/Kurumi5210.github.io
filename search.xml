<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>PD 分离场景下的 KV Cache 传输优化</title>
      <link href="/2026/07/10/kv-transfer-optimization/"/>
      <url>/2026/07/10/kv-transfer-optimization/</url>
      
        <content type="html"><![CDATA[<blockquote><p>本文记录一次 PD 分离场景下 Mooncake Connector 的 KV Cache 传输优化，重点是 Qwen3 等 GQA&#x2F;MHA&#x2F;MQA 模型下的 TP 异构、PP 适配、ACL Graph 稳定性问题，以及后续融合 transpose 算子的设计。</p></blockquote><h2 id="背景：从-MLA-到-GQA"><a href="#背景：从-MLA-到-GQA" class="headerlink" title="背景：从 MLA 到 GQA"></a>背景：从 MLA 到 GQA</h2><p>在 6 月 30 日，我们完成了 PD 分离场景下的 Mooncake Connector，主要用于 Prefill 节点到 Decode 节点之间的 KV Cache 传输。初版主要面向 DeepSeek 模型开发，它的 attention 使用 MLA（Multi-Head Linear Attention），KV Cache 的组织方式有两个比较友好的特点：</p><ol><li>KV Cache 由 embedding 之后的 hidden states 经过下投影矩阵生成。</li><li>在 TP（Tensor Parallel）域内，各个 TP rank 可以缓存全量 KV Cache。Decode 节点拉取 KV Cache 时，选择哪个 Prefill TP rank 都是可行的。</li></ol><p>随着 Qwen3 适配推进，GQA、MHA、MQA 场景把问题复杂化了。核心变化是：TP 域内的 KV Cache 不再统一。Decode 节点不能再随便挑一个 Prefill TP rank 拉数据，而是必须选择正确的 rank，并且在 Decode 节点的 local block 内完成重排序。</p><p>这带来两个直接挑战：</p><ul><li><strong>TP 异构下的 KV Cache 不一致</strong>：Prefill 和 Decode 的 TP 配置不同，拉到 Decode 侧的数据排布不能直接用于 forward。</li><li><strong>PP 场景下的层归属不同</strong>：拉取 KV Cache 时必须考虑 Prefill 节点的 PP stage，不同 stage 持有不同层的 KV Cache，即使 MLA 场景也不能忽略这一点。</li></ul><p>这次优化的目标很明确：</p><ol><li>在不明显影响性能的前提下，让 PD 分离支持 GQA TP 异构的 KV Cache 传输。</li><li>支持 PP（Pipeline Parallel）场景下的 KV Cache 传输。</li><li>支持开启 MTP（Multi-Token Prefill）后的 PP 流水线。</li></ol><h2 id="Decode-侧-KV-Cache-重排序"><a href="#Decode-侧-KV-Cache-重排序" class="headerlink" title="Decode 侧 KV Cache 重排序"></a>Decode 侧 KV Cache 重排序</h2><p>GQA 格式下，P 节点和 D 节点 TP 配置不同，P 节点生成的 KV Cache 与 D 节点期望的存储排布不一致。以 Qwen3 235B 为例，当 Prefill 侧 TP size 为 16、<code>num_heads = 4</code> 时，单个 TP rank 只能生成一部分 head 对应的 KV Cache。Decode 侧如果直接使用这些数据，forward 的 layout 就是错的。</p><p>解决思路是把 layout 转换放到 Connector 层完成：</p><ol><li>以 block 为粒度对 KV Cache 做重排序，reshape 成符合 Decode 侧 TP size 的排布。</li><li>vLLM Connector 感知 TP 配置，并完成 KV Cache 的 split 和 cat。</li><li>底层 Mooncake TransferEngine 只负责数据传输，不感知 TP 配置。</li><li>为避免额外申请大 buffer，引入逐 block 的 zero-copy 传输，并在传输完成后逐 block transpose，达到 cat 的效果。</li></ol><p><img src="/images/KVOptimization/gqa-tp-layout-reorder.png" alt="GQA TP 异构下的 KV Cache 重排序"></p><p>重排序的执行流程如下：</p><p><img src="/images/KVOptimization/cache-reorder-flow.png" alt="KV Cache 重排序流程"></p><p>初版逐 block 重排序在 4K KV Cache 上约 120ms。由于每层 Decode 间需要频繁 launch kernel，在 4K x 1.5K benchmark 中，TPOT 增加了约 28ms。</p><p>后续优化改成使用 <code>_npu_page_load</code> 读取 KV Cache，再用 <code>_npu_reshape_and_cache</code> 写回。这样每层只处理一次，只需要申请一层 KV Cache 大小的 buffer，大幅减少了 kernel launch 时间。</p><h2 id="选择-Prefill-Rank"><a href="#选择-Prefill-Rank" class="headerlink" title="选择 Prefill Rank"></a>选择 Prefill Rank</h2><p>Decode 节点拉取 KV Cache 时，需要选择对应的 Prefill TP rank。这个选择逻辑在 MLA 和 GQA 场景下不一样：</p><ul><li>MLA 场景下，同一 TP 域内的 rank 可以缓存全量 KV Cache，因此可以随机选择 Prefill TP rank，用于动态负载均衡。</li><li>GQA 场景下，TP rank 对应的数据分片不同，Decode 侧必须选择正确的 Prefill TP rank。</li><li>PP 场景下，需要先按 PP stage 对 Prefill 节点分组，再在每个 PP stage 内选择 TP rank。</li></ul><p><img src="/images/KVOptimization/prefill-rank-selection.png" alt="Prefill TP Rank 选择逻辑"></p><p>这部分改造的关键是让 Connector 同时感知 TP 和 PP 配置。TP 维度决定从哪个 rank 拉，PP 维度决定这个 rank 上有哪些 layer 需要拉。</p><h2 id="KV-Cache-传输路径"><a href="#KV-Cache-传输路径" class="headerlink" title="KV Cache 传输路径"></a>KV Cache 传输路径</h2><p>真正的数据搬运仍然交给 Mooncake TransferEngine。Connector 负责根据源地址和目标地址列表构造传输任务，并把正确的地址偏移计算好。</p><p>在 GQA TP 切分下，Decode 节点每层可能需要多次拉取 KV Cache。每次拉取的数据都要写入 Decode 侧 local block 的正确位置，因此必须引入地址 offset。</p><p>PP 场景下，单个 Prefill rank 只持有一部分 layer 的 KV Cache。Connector 需要根据 PP stage 和 <code>get_pp_indices</code> 计算本次传输涉及的 layer，并将它们放到 Decode 节点对应地址。</p><p><img src="/images/KVOptimization/kv-transfer-flow.png" alt="KV Cache 传输流程"></p><p>这里的职责划分很重要：TransferEngine 只接受源地址和目标地址并完成传输，复杂的 TP&#x2F;PP layout 逻辑收敛在 Connector 内部。</p><h2 id="手动划分-PP-Layer"><a href="#手动划分-PP-Layer" class="headerlink" title="手动划分 PP Layer"></a>手动划分 PP Layer</h2><p>开启 MTP 后，KV Cache 会多出 MTP 层，原有 <code>get_pp_indices</code> 无法感知这部分 layer，容易导致部分 KV Cache 未传输，或者 PP 层切分不均。</p><p>为了解决这个问题，我们引入了手动 PP layer partition，通过 Connector 配置让 Mooncake Connector 感知层数切分。例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> VLLM_PP_LAYER_PARTITION=33,28</span><br></pre></td></tr></table></figure><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;kv_connector_extra_config&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;use_ascend_direct&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;prefill&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;dp_size&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;tp_size&quot;</span><span class="punctuation">:</span> <span class="number">8</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;pp_size&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;pp_layer_partition&quot;</span><span class="punctuation">:</span> <span class="string">&quot;33,28&quot;</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;decode&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;dp_size&quot;</span><span class="punctuation">:</span> <span class="number">16</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;tp_size&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;pp_size&quot;</span><span class="punctuation">:</span> <span class="number">1</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这相当于把 PP layer 的切分结果显式交给 Connector，避免 Connector 只依赖默认 <code>get_pp_indices</code> 推断。</p><h2 id="开发中遇到的几个问题"><a href="#开发中遇到的几个问题" class="headerlink" title="开发中遇到的几个问题"></a>开发中遇到的几个问题</h2><h3 id="Decode-重排序精度问题"><a href="#Decode-重排序精度问题" class="headerlink" title="Decode 重排序精度问题"></a>Decode 重排序精度问题</h3><p>最初版本中出现过一个精度问题：前半段数据正确，后半段数据错乱。根因是重排序和结束信号的时序不清晰，部分数据还没有完成拉取或重排，就进入了后续流程。</p><p>修复方式是把流程边界收紧：所有 KV Cache 拉取完成后才开始重排序，重排序完成后再通知 Prefill 侧释放显存。</p><h3 id="Mooncake-对非对等层数传输的支持问题"><a href="#Mooncake-对非对等层数传输的支持问题" class="headerlink" title="Mooncake 对非对等层数传输的支持问题"></a>Mooncake 对非对等层数传输的支持问题</h3><p>PP KV Cache 传输初版遇到过访存越界。原因是当时 Mooncake 版本会根据首地址和本端注册的层数进行拉取，而 PP 场景下两端持有的 layer 数并不总是对等，报错信息里可以看到循环首地址。</p><p>后续 Mooncake 版本更新后解决了这个问题。</p><h2 id="性能收益"><a href="#性能收益" class="headerlink" title="性能收益"></a>性能收益</h2><p>优化后的收益主要体现在两部分：</p><ol><li><code>_reshape</code> 替换 <code>_copy</code> 后，TPOT 从 120ms 降至 94ms。</li><li>在无异步调度时，重排序几乎没有额外 TPOT 损耗。</li></ol><p>PP 适配完成后，DeepSeek PD 分离的性能瓶颈从 Prefill 节点转移到 Decode 节点。调整 PD 分离配比后，归一化 QPS 还能继续提升。</p><h2 id="ACL-Graph-支持-Qwen3-235B-PD-分离"><a href="#ACL-Graph-支持-Qwen3-235B-PD-分离" class="headerlink" title="ACL Graph 支持 Qwen3 235B PD 分离"></a>ACL Graph 支持 Qwen3 235B PD 分离</h2><p>在 Mooncake Connector 与 vLLM 结合的场景下，forward 流程和 Mooncake cat 方法中都使用了 <code>torch_npu._npu_reshape_and_cache</code> 算子。问题表现为：ACL Graph 场景下，调用 vLLM 的 connect 时偶发 cancel 异常。</p><p>这两个算子在功能上等价，但一个在图内调用，另一个在图外调用：</p><ul><li>forward 内部入图的 <code>_npu_reshape_and_cache</code> 与 Mooncake cat 中的 <code>_npu_reshape_and_cache</code> 会互相影响。</li><li>两处算子分别替换成等效实现都能正常跑通，精度也正常。</li><li>注释任意一个算子，也能正常运行。</li><li>同时保留两者时，可能抛出异常或出现不稳定行为。</li></ul><p>异常形式也和调度方式有关：</p><ul><li>ACL Graph + <code>--async-scheduling</code> 时，vLLM connect 抛出 cancel 异常，没有更具体的错误信息。</li><li>关闭 <code>--async-scheduling</code> 后，异常能定位到 forward 内部的 <code>_npu_reshape_and_cache</code>。</li></ul><p>根因在 PageAttention 算子更新过程。算子更新时每次需要额外申请 workspace，这个 workspace 来自单算子流的 pool。算子更新会把 workspace 写入图中，更新结束后 workspace 被释放。但是图重放时，单算子流正在进行 KV Cache 传输；从单算子流视角看，这段 workspace 可以复用，但从图视角看，PageAttention 计算仍然需要这段 workspace，于是发生了内存踩踏。</p><p>规避方案是在 slot mapping 生成后加入同步操作，改变流上的算子排布逻辑，以及图运行和下发的时序。</p><h2 id="融合-transpose-kv-cache-by-block-算子"><a href="#融合-transpose-kv-cache-by-block-算子" class="headerlink" title="融合 transpose_kv_cache_by_block 算子"></a>融合 transpose_kv_cache_by_block 算子</h2><p>当前 GQA 场景下的 layout 转换依赖 <code>_npu_reshape_and_cache</code> 和 <code>_npu_page_load</code>。这个方案能跑通，但仍然存在两个问题：</p><ol><li>算子调用次数过多。目前每层需要 launch 4 个算子，一个请求会调用 376 次算子。</li><li>当前依赖 Decode 间的 bubble 掩盖算子开销。未来异步调度优化 Decode bubble 后，这部分开销可能无法继续被掩盖。</li></ol><p>本质上，这里要做的是一次 transpose，只是现有算子无法一次性完成。因此可以实现一个融合算子，一次完成所有层 KV Cache 的 layout 转换。</p><h3 id="算子接口"><a href="#算子接口" class="headerlink" title="算子接口"></a>算子接口</h3><p>算子的功能是：对传入的 <code>k_cache</code> 和 <code>v_cache</code> TensorList 进行 layout 转换。每隔 <code>block_len</code> 个数据视为一个 block，按照 <code>block_ids</code> 指定的 block 做转换。每个 block 内包含 <code>split_num</code> 份数据，每份表示 <code>num_head / split_num</code> 个 head 的 KV Cache。转换完成后，把这 <code>split_num</code> 份数据在 <code>num_head</code> 维度合并。</p><p>这个算子将结果写回原内存，不额外使用 workspace。</p><p>接口定义如下：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">transpose_kv_cache_by_block(</span><br><span class="line">    k_cache,</span><br><span class="line">    v_cache,</span><br><span class="line">    block_ids,</span><br><span class="line">    block_size,</span><br><span class="line">    head_sum,</span><br><span class="line">    head_dim,</span><br><span class="line">    split_num,</span><br><span class="line">    layer_num,</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>算子描述可以抽象成：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">[</span></span><br><span class="line">  <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;op&quot;</span><span class="punctuation">:</span> <span class="string">&quot;TransposeKVCacheByBlock&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;input_desc&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;KCache&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;format&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;ND&quot;</span><span class="punctuation">,</span> <span class="string">&quot;ND&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;fp16&quot;</span><span class="punctuation">,</span> <span class="string">&quot;bf16&quot;</span><span class="punctuation">]</span></span><br><span class="line">      <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;VCache&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;format&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;ND&quot;</span><span class="punctuation">,</span> <span class="string">&quot;ND&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;fp16&quot;</span><span class="punctuation">,</span> <span class="string">&quot;bf16&quot;</span><span class="punctuation">]</span></span><br><span class="line">      <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;blockIDs&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;format&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;ND&quot;</span><span class="punctuation">,</span> <span class="string">&quot;ND&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;int32&quot;</span><span class="punctuation">,</span> <span class="string">&quot;int32&quot;</span><span class="punctuation">]</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;output_desc&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;attr&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">      <span class="punctuation">&#123;</span><span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;blockSize&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;int&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span><span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;headNum&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;int&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span><span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;headDim&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;int&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span><span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;splitNum&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;int&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="punctuation">&#123;</span><span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;layerNum&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;param_type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;required&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;int&quot;</span><span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure><p>当 <code>num_head = 4</code>、<code>split_num = 4</code> 时，转换过程如下：</p><p><img src="/images/KVOptimization/transpose-split4.png" alt="split_num 为 4 的 KV Cache transpose"></p><p>当 <code>num_head = 4</code>、<code>split_num = 2</code> 时，转换过程如下：</p><p><img src="/images/KVOptimization/transpose-split2.png" alt="split_num 为 2 的 KV Cache transpose"></p><h3 id="实现设计"><a href="#实现设计" class="headerlink" title="实现设计"></a>实现设计</h3><p>首先根据 <code>blockIDs</code> 计算每个 layer 有多少个 block 需要做 layout 转换：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">block_num = blockIDs.shape[0]</span><br><span class="line">total_block_num = layerNum * block_num</span><br><span class="line">data_size_load_once = block_size * head_num * head_dim * sizeof(T)</span><br></pre></td></tr></table></figure><p>如果 <code>data_size_load_once</code> 单个 core 就可以完成读取，则使用模板一；否则使用模板二。</p><h4 id="模板一：单-core-处理一个-block"><a href="#模板一：单-core-处理一个-block" class="headerlink" title="模板一：单 core 处理一个 block"></a>模板一：单 core 处理一个 block</h4><p>模板一的特点是单个 core 可以完整读取一个 block 的所有数据。整体流程是把整个 block 读入 UB，在 UB 中完成重排，然后直接写回原内存位置。</p><p>负载均衡时，先计算每个 core 至少需要处理多少个 block：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">round = total_block_num / vector_core_num</span><br><span class="line">tailCoreNum = total_block_num % vector_core_num</span><br></pre></td></tr></table></figure><p>有 <code>tailCoreNum</code> 个 core 需要多处理一个 block。每个 core 负责的 block 可能分布在不同 layer 中，这样能让负载更均衡。</p><p>重排的关键是 <code>DataCopyParams</code>：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">DataCopyParams repeatParams;</span><br><span class="line">repeatParams.blockCount = blockSize_;</span><br><span class="line">repeatParams.blockLen =</span><br><span class="line">    headNum_ / splitNum_ * headDim_ * <span class="built_in">sizeof</span>(T) / dataBlockSize_;</span><br><span class="line">repeatParams.srcStride = <span class="number">0</span>;</span><br><span class="line">repeatParams.dstStride =</span><br><span class="line">    (headNum_ * headDim_ - headNum_ / splitNum_ * headDim_) *</span><br><span class="line">    <span class="built_in">sizeof</span>(T) / dataBlockSize_;</span><br></pre></td></tr></table></figure><p>一条 <code>DataCopy</code> 指令完成 <code>repeatParams.blockCount * repeatParams.blockLen</code> 的搬运量，也就是总数据量的 <code>1 / splitNum_</code>。因此需要执行 <code>splitNum_</code> 次 <code>DataCopy</code>，每次偏移已经完成搬运的部分：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">dstFactor_ = headNum_ / splitNum_ * headDim_;</span><br><span class="line">srcFactor_ = blockSize_ * headNum_ / splitNum_ * headDim_;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="type">uint32_t</span> i = <span class="number">0</span>; i &lt; splitNum_; ++i) &#123;</span><br><span class="line">    <span class="built_in">DataCopy</span>(cacheLocal[i * dstFactor_],</span><br><span class="line">             cacheGm[i * srcFactor_ + offsetBlock],</span><br><span class="line">             repeatParams);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 UB 内完成重排后，再连续搬出即可。</p><h4 id="模板二：多-core-协同处理一个-block"><a href="#模板二：多-core-协同处理一个-block" class="headerlink" title="模板二：多 core 协同处理一个 block"></a>模板二：多 core 协同处理一个 block</h4><p>模板二解决的是单个 core 无法读取完整 block 的场景。多个 core 协同读取一个 block，这里的协同 core 数称为 <code>blockSizeSplitNum</code>。</p><p>为了充分利用算力，<code>blockSizeSplitNum</code> 取 AIV core 数的因子。以 48 个 core 为例，可选值包括 2、3、4、6、8、12、16、24、48。实现上选择能满足读取一个 block 的最小因子；如果最大因子也无法满足，就说明超出了算子能力，需要拦截并报错。</p><p>负载均衡公式变为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">core_num = vector_core_num / blockSizeSplitNum</span><br><span class="line">round = total_block_num / core_num</span><br><span class="line">tailCoreNum = total_block_num % core_num</span><br></pre></td></tr></table></figure><p>模板二和模板一的主要差异在于 block_size 维度被切分，<code>DataCopyParams</code> 需要修正：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">blockSizePerTime_ = blockSize / blockSizeSplitNum;</span><br><span class="line">repeatParams.blockCount = blockSizePerTime_;</span><br></pre></td></tr></table></figure><p>多个 core 计算同一个 block 时，搬入和搬出地址都需要加上对应偏移：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">uint32_t</span> blockSizeIndex = blockIdx_ % blockSizeSplitNum_;</span><br><span class="line">srcOffset = blockSizeIndex * blockSizePerTime_ * headNumSplited_ * headDim_;</span><br><span class="line">dstOffset = blockSizeIndex * blockSizePerTime_ * headNum_ * headDim_;</span><br><span class="line"></span><br><span class="line"><span class="built_in">CopyIn</span>(kCacheGm_, offsetBlock + srcOffset, repeatParams);</span><br><span class="line"><span class="built_in">CopyOut</span>(kCacheGm_, offsetBlock + dstOffset);</span><br></pre></td></tr></table></figure><p>搬出前需要做一次核间同步，保证所有 core 都已经完成读入，避免覆盖仍在读取的数据。</p><h3 id="性能实测"><a href="#性能实测" class="headerlink" title="性能实测"></a>性能实测</h3><p>使用融合算子后，reshape KV Cache 的时间开销从 UT 中的 7ms 降低到 0.24ms。在 PD 分离场景下，Qwen3 235B 的 TTFT 可降低约 90ms 到 110ms。</p><p><img src="/images/KVOptimization/transpose-benchmark.jpeg" alt="transpose_kv_cache_by_block 性能实测"></p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>这次 KV Cache 传输优化的核心不是单纯把数据从 Prefill 搬到 Decode，而是把 TP、PP、模型 attention 结构和执行流同步问题都纳入 Connector 的控制面。</p><p>几个关键经验：</p><ol><li><strong>TransferEngine 不应该感知模型并行细节</strong>。TP&#x2F;PP layout 逻辑放在 Connector 层，底层通信库只负责可靠搬运。</li><li><strong>GQA TP 异构必须显式处理 layout</strong>。MLA 场景中随机选 rank 的假设，不能直接复用到 GQA&#x2F;MHA&#x2F;MQA。</li><li><strong>PP stage 是 KV Cache 路由的一部分</strong>。只看 TP rank 不够，必须同时知道 layer 属于哪个 PP stage。</li><li><strong>性能瓶颈常常来自 kernel launch 次数，而不只是搬运带宽</strong>。从逐 block 重排序到 <code>_npu_page_load</code> + <code>_npu_reshape_and_cache</code>，再到融合算子，本质都是在减少不必要的 launch 和中间搬运。</li><li><strong>ACL Graph 与异步流的内存生命周期需要特别谨慎</strong>。图内 workspace、图外单算子流和 KV 传输并行时，必须保证同步边界清晰。</li></ol><p>最终结果是，PD 分离可以覆盖 Qwen3 235B 这类 GQA 模型和 PP&#x2F;MTP 场景，同时通过融合算子把 layout 转换开销压到更可控的范围内。</p>]]></content>
      
      
      <categories>
          
          <category> 推理优化 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> vLLM </tag>
            
            <tag> Ascend </tag>
            
            <tag> KV Cache </tag>
            
            <tag> 推理优化 </tag>
            
            <tag> Mooncake </tag>
            
            <tag> PD 分离 </tag>
            
            <tag> GQA </tag>
            
            <tag> Pipeline Parallel </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2026/06/04/hello-world/"/>
      <url>/2026/06/04/hello-world/</url>
      
        <content type="html"><![CDATA[<p>欢迎来到 chenxiao 的博客 🎉</p><p>这是我的第一篇博客文章。这个博客使用 <a href="https://hexo.io/">Hexo</a> 和 <a href="https://butterfly.js.org/">Butterfly</a> 主题搭建，部署在 GitHub Pages 上。</p><h2 id="关于我"><a href="#关于我" class="headerlink" title="关于我"></a>关于我</h2><p>一个热爱编程和技术探索的开发者，关注 CUDA 编程、深度学习推理优化和系统设计。</p><h2 id="博客计划"><a href="#博客计划" class="headerlink" title="博客计划"></a>博客计划</h2><p>后续会陆续更新以下内容：</p><ul><li>CUDA 编程学习笔记</li><li>深度学习推理优化实践</li><li>系统设计相关文章</li><li>技术杂谈</li></ul><p>敬请期待！</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">&quot;Hello, chenxiao&#x27;s blog!&quot;</span>)</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 技术随笔 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 博客 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>为 vLLM-Ascend 实现节点级 CPU Prefix Cache——设计、实现与踩坑记录</title>
      <link href="/2026/06/04/vllm-ascend-cpu-offloading/"/>
      <url>/2026/06/04/vllm-ascend-cpu-offloading/</url>
      
        <content type="html"><![CDATA[<blockquote><p>项目 PR：<a href="https://github.com/vllm-project/vllm-ascend/pull/1659">vllm-project&#x2F;vllm-ascend#1659</a></p></blockquote><h2 id="为什么需要-CPU-Prefix-Cache"><a href="#为什么需要-CPU-Prefix-Cache" class="headerlink" title="为什么需要 CPU Prefix Cache"></a>为什么需要 CPU Prefix Cache</h2><p>大模型推理服务有几个关键指标：首 token 延迟（TTFT）、系统吞吐（QPS）、单位请求成本。在真实业务中，用户请求之间存在大量可复用的公共前缀——系统提示词、工具调用模板、多轮对话历史、RAG 检索结果、批量任务中的相同输入片段。如果能把这些前缀的 KV Cache 缓存起来复用，就可以跳过重复的 prefill 计算，直接降低 TTFT、提升吞吐。</p><p>vLLM V1 已经支持同节点内 HBM Prefix Cache 的复用，但问题在于：昇腾 NPU 的 HBM 是极其紧缺的资源。模型权重加载完后，留给 KV Cache 的空间很有限；在长上下文、高并发、大 batch 场景下，HBM KV Cache 很快会被活跃请求占满，能长期保留的 Prefix Cache 空间严重不足。所以 HBM Prefix Cache 虽然快，但在生产场景中容量受限，很难形成稳定有效的缓存命中。</p><p>那么很自然的想法就是——把 CPU DRAM 用起来。CPU 内存容量大、成本低，虽然访问速度不如 HBM，但适合保存那些访问频率相对较低、但仍然有复用价值的 Prefix KV Cache。通过异步将 KV Cache 从 HBM 搬运到 CPU DRAM，后续请求命中时再按需回迁，在不增加 HBM 常驻压力的前提下扩大 Prefix Cache 的可复用范围。</p><p>这就是我们做 CPU Offloading 的出发点。</p><h2 id="几个关键设计决策"><a href="#几个关键设计决策" class="headerlink" title="几个关键设计决策"></a>几个关键设计决策</h2><p>在动手之前，我们定了几个基本约束，这些约束直接影响了后续的架构选择。</p><h3 id="节点级共享，不跨节点"><a href="#节点级共享，不跨节点" class="headerlink" title="节点级共享，不跨节点"></a>节点级共享，不跨节点</h3><p>CPU DRAM Prefix Cache 的共享范围是单节点。不同物理节点之间不共享，但同一节点内不同 DP 域之间可以共享。这个设计天然要求一个前提：<strong>同一个 DP 域内的 Worker 必须部署在同一物理节点上</strong>。如果 Worker 跨节点分布，同一请求依赖的 KV Cache 就会分散在不同节点，引入跨节点拉取、缓存一致性维护和额外网络开销，得不偿失。</p><p>所以我们在部署层面将 DP 域和节点边界绑定——每个 DP 域内的 Worker 位于同一节点，CPU DRAM Prefix Cache 作为节点内部的共享缓存层。</p><h3 id="低侵入式设计，复用-KVConnector-架构"><a href="#低侵入式设计，复用-KVConnector-架构" class="headerlink" title="低侵入式设计，复用 KVConnector 架构"></a>低侵入式设计，复用 KVConnector 架构</h3><p>vLLM V1 提供了 <code>KVConnectorBase_V1</code> 抽象基类和 <code>KVConnectorModelRunnerMixin</code>，这给了我们一个很好的扩展点。我们的目标是：<strong>对 Scheduler 和 Worker 原生代码零改动</strong>，CPU Offloading 逻辑全部封装在 Connector 模块和配套的 MetadataServer 进程中。</p><p>这样做的好处很明显：不影响 vLLM 主体执行流程、方便独立维护和开关控制、后续版本升级的适配成本也更低。</p><h3 id="异步逐层-swap-out，不阻塞主推理路径"><a href="#异步逐层-swap-out，不阻塞主推理路径" class="headerlink" title="异步逐层 swap-out，不阻塞主推理路径"></a>异步逐层 swap-out，不阻塞主推理路径</h3><p>在 prefill 过程中，每层生成新的 KV Cache 后立即触发异步拷贝到 CPU DRAM，让数据搬运与后续层的 forward 计算形成流水重叠，而不是等全部 forward 结束后集中搬运。不过，forward 结束后 Worker 主线程仍然需要同步等待所有层的 swap-out 完成——这是为了保证 CPU DRAM 中的数据完整可用，避免后续请求命中到写了一半的缓存。</p><h3 id="命中增益阈值，避免”亏本”的-swap-in"><a href="#命中增益阈值，避免”亏本”的-swap-in" class="headerlink" title="命中增益阈值，避免”亏本”的 swap-in"></a>命中增益阈值，避免”亏本”的 swap-in</h3><p>从 CPU DRAM 回迁 KV Cache 到 HBM 是有开销的。如果 CPU 命中的前缀只比 HBM 多一点点，数据搬运的代价可能超过跳过的 prefill 收益。所以我们引入了一个阈值 k：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CPU DRAM 命中长度 - HBM 命中长度 &gt;= k</span><br></pre></td></tr></table></figure><p>只有满足这个条件才触发 swap-in。阈值可以按模型、上下文长度、业务负载和硬件配置灵活调整，在命中收益和搬运开销之间取得平衡。</p><h2 id="整体架构"><a href="#整体架构" class="headerlink" title="整体架构"></a>整体架构</h2><p><img src="/images/CPUoffloading/%E6%9E%B6%E6%9E%84.png" alt="整体架构图"></p><p>架构的核心思路是”控制面与数据面分离”：</p><ul><li><strong>控制面</strong>（Scheduler + MetadataServer）：负责缓存匹配、空间分配、元信息构造和缓存生命周期管理。</li><li><strong>数据面</strong>（Worker + NPU copy stream）：只根据元信息执行实际的数据搬运，不参与缓存哈希和 block 管理。</li></ul><p>我们在每个节点上启动一个独立的 MetadataServer 进程，管理共享 CPU KV Cache 空间、block 分配、哈希索引、引用计数和命中统计；新增的 <code>CPUOffloadingConnector</code> 则嵌入在 vLLM V1 的 KVConnector 扩展点中，对上层 Scheduler 和 Worker 保持透明。</p><p>Scheduler 侧和 Worker 侧各暴露 3 个接口：</p><table><thead><tr><th>侧</th><th>接口</th><th>职责</th></tr></thead><tbody><tr><td>Scheduler</td><td><code>get_num_new_matched_tokens</code></td><td>查询 CPU DRAM 命中长度</td></tr><tr><td>Scheduler</td><td><code>build_connector_meta</code></td><td>构造本轮调度元信息（CPU block 分配、GPU block 映射等）</td></tr><tr><td>Worker</td><td><code>bind_connector_metadata</code></td><td>绑定 Scheduler 下发的缓存元信息</td></tr><tr><td>Worker</td><td><code>start_load_kv</code></td><td>启动 DRAM → HBM 的 KV Cache 加载</td></tr><tr><td>Worker</td><td><code>wait_for_layer_load</code></td><td>每层 forward 前等待当前层加载完成，触发下一层加载</td></tr></tbody></table><p>这样 Scheduler 只管”查缓存、分配空间、构造元信息”，Worker 只管”按元信息搬运数据”，MetadataServer 管”全局共享内存和缓存生命周期”，三个角色的职责非常清晰。</p><h2 id="核心实现"><a href="#核心实现" class="headerlink" title="核心实现"></a>核心实现</h2><h3 id="MetadataServer：节点级的缓存大脑"><a href="#MetadataServer：节点级的缓存大脑" class="headerlink" title="MetadataServer：节点级的缓存大脑"></a>MetadataServer：节点级的缓存大脑</h3><p>MetadataServer 是每个物理节点上唯一的控制进程（<code>metadata_server.py</code>），启动时根据 <code>cpu_swap_space_gb</code> 配置一次性创建共享内存池（默认 800 GB），统一管理本节点内的 CPU KV Cache 空间。</p><p>它对外暴露六个核心接口，配合完成缓存的全生命周期管理：</p><ul><li><strong><code>init_cpu_kv_caches</code></strong>：每个 ConnectorWorker 启动时调用，以 <code>(pp_rank, tp_rank)</code> 为维度初始化共享内存视图。首次出现的组合创建共享内存，已存在的直接复用，保证同节点内相同并行配置的 Worker 共享一致的缓存空间。</li><li><strong><code>post_init</code></strong>：全部 Worker 初始化完成后由 ConnectorScheduler 触发，整个节点生命周期内只执行一次，实例化 <code>CPUKVCacheManager</code> 并注册 RPC 路由表。</li><li><strong><code>get_matched_num_and_touch</code></strong>：调度前查询命中长度，同时对命中 block 增加引用计数，防止在 swap-in 完成前被提前回收。</li><li><strong><code>allocate_slots</code> &#x2F; <code>cache_and_free_slots</code></strong>：两阶段缓存提交机制。<code>allocate_slots</code> 只分配空间但不标记可用；等 Worker 侧 save 线程完成数据拷贝后，<code>cache_and_free_slots</code> 才将 block 写入哈希表成为可命中缓存。这保证了后续请求永远不会命中写入未完成的脏数据。</li><li><strong><code>free_slot</code></strong>：请求结束时引用计数减一，降到 0 才真正回收。正确处理了多请求复用、提前 abort、新分配未提交等边界场景。</li></ul><p>这里有两个我觉得值得展开的细节。</p><h4 id="两阶段提交"><a href="#两阶段提交" class="headerlink" title="两阶段提交"></a>两阶段提交</h4><p>为什么要把分配和标记拆成两步？因为在 <code>allocate_slots</code> 执行时，Worker 侧还没开始做 HBM → DRAM 的拷贝，CPU DRAM 里的数据是无效的。如果这时候就把 block 标记为可命中，后续请求命中后读到的是垃圾数据，推理就错了。所以必须等 Worker 后台 save 线程完成拷贝、通过完成队列通知 Metadata 后，才能把 block 写入 Prefix Cache 哈希表。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># allocate_slots: 只分配，不标记</span></span><br><span class="line"><span class="comment"># cache_and_free_slots: 确认写入完成，标记为可复用</span></span><br></pre></td></tr></table></figure><p>这个设计保证了 Prefix Cache 的一致性：<strong>只有完成数据写入的 block 才允许被命中</strong>。</p><h4 id="引用计数保障生命周期安全"><a href="#引用计数保障生命周期安全" class="headerlink" title="引用计数保障生命周期安全"></a>引用计数保障生命周期安全</h4><p>节点级共享 Prefix Cache 意味着多个请求可能同时引用同一段缓存。引用计数机制保证了：</p><ul><li>同一段 Prefix Cache 被多个请求复用时，每个请求持有各自的引用；</li><li>某个请求提前 abort，不影响其他还在使用该缓存的请求；</li><li>新分配但尚未提交为 Prefix Cache 的 block 也有初始引用计数；</li><li>只有引用计数归零，block 才被回收到 pool。</li></ul><h3 id="CPUOffloadingWorker：数据搬运的流水线"><a href="#CPUOffloadingWorker：数据搬运的流水线" class="headerlink" title="CPUOffloadingWorker：数据搬运的流水线"></a>CPUOffloadingWorker：数据搬运的流水线</h3><p>数据搬运是 CPU Offloading 的数据面核心，有两类路径：DRAM → HBM（swap-in）和 HBM → DRAM（swap-out）。为了不让数据搬运阻塞模型计算，我们为 load 和 save 分别创建了独立的 NPU stream。</p><h4 id="Swap-in：逐层加载，降低-TTFT-影响"><a href="#Swap-in：逐层加载，降低-TTFT-影响" class="headerlink" title="Swap-in：逐层加载，降低 TTFT 影响"></a>Swap-in：逐层加载，降低 TTFT 影响</h4><p>当请求命中 CPU DRAM Prefix Cache 且命中增益超过阈值后，需要把命中的 KV Cache 从 CPU DRAM 回迁到 NPU HBM。如果等所有层的 KV Cache 全部加载完再开始 forward，TTFT 会被严重拖累。</p><p>我们的做法是<strong>逐层流水线加载</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">start_load_kv</span>(<span class="params">self</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">    <span class="variable language_">self</span>.current_layer = <span class="number">0</span></span><br><span class="line">    <span class="variable language_">self</span>.gpu_kv_caches_load_iter = <span class="built_in">iter</span>(<span class="variable language_">self</span>.gpu_kv_caches.values())</span><br><span class="line">    <span class="variable language_">self</span>.load_kv_layer(<span class="number">0</span>)  <span class="comment"># 立即发起第 0 层加载</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">wait_for_layer_load</span>(<span class="params">self</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">    <span class="variable language_">self</span>.load_stream.synchronize()  <span class="comment"># 等待当前层加载完成</span></span><br><span class="line">    <span class="variable language_">self</span>.current_layer += <span class="number">1</span></span><br><span class="line">    <span class="variable language_">self</span>.load_kv_layer(<span class="variable language_">self</span>.current_layer)  <span class="comment"># 发起下一层加载</span></span><br></pre></td></tr></table></figure><p>执行流程变成：</p><ol><li>发起第 0 层 KV 加载</li><li>等待第 0 层完成 → 执行第 0 层 forward → 发起第 1 层加载</li><li>等待第 1 层完成 → 执行第 1 层 forward → 发起第 2 层加载</li><li>…</li></ol><p>这样只要求当前即将计算的层的 KV Cache 已经就位，不必等所有层一次性回迁完毕，大幅降低了集中式 swap-in 的前置等待时间。</p><blockquote><p>当前实现用 <code>self.load_stream.synchronize()</code> 做同步，后续可以换成 <code>torch.npu.current_stream().wait_stream(self.load_stream)</code>，避免不必要的全流同步，进一步降低开销。</p></blockquote><h4 id="Swap-out：后台线程-独立流，不阻塞主路径"><a href="#Swap-out：后台线程-独立流，不阻塞主路径" class="headerlink" title="Swap-out：后台线程 + 独立流，不阻塞主路径"></a>Swap-out：后台线程 + 独立流，不阻塞主路径</h4><p>Swap-out 用来把新生成的 KV Cache 保存到 CPU DRAM。为了避免阻塞 Worker 主线程，我们在 Connector 初始化时启动一个后台 save 线程，监听 <code>save_input_queue</code>：</p><ol><li>Worker 主线程提交待保存请求到队列</li><li>后台线程计算本次新增需保存的 block 范围</li><li>在独立 <code>save_stream</code> 上异步执行 HBM → DRAM copy</li><li>同步等待拷贝完成</li><li>将 <code>req_id</code> 写入 <code>save_output_queue</code></li><li>MetadataServer 标记对应 block 为可复用</li></ol><p>计算增量 block 范围的逻辑：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(</span><br><span class="line">        req.num_cpu_computed_tokens // <span class="variable language_">self</span>.block_size,</span><br><span class="line">        <span class="built_in">min</span>((req.num_computed_tokens + req.num_scheduled_tokens) //</span><br><span class="line">            <span class="variable language_">self</span>.block_size, <span class="built_in">len</span>(req.cpu_block_ids))):</span><br><span class="line">    save_block_mapping.append(</span><br><span class="line">        (req.gpu_block_ids[i], req.cpu_block_ids[i]))</span><br></pre></td></tr></table></figure><p>这样只保存新增产生且尚未写入 CPU 的 block，避免重复搬运。</p><h4 id="MLA-与非-MLA-的适配"><a href="#MLA-与非-MLA-的适配" class="headerlink" title="MLA 与非 MLA 的适配"></a>MLA 与非 MLA 的适配</h4><p>MLA 和非 MLA 模型在张量并行下的 KV Cache 组织方式不同，需要在保存时分别处理：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="variable language_">self</span>.use_mla:</span><br><span class="line">    start, step = <span class="variable language_">self</span>.tp_rank, <span class="variable language_">self</span>.tp_world_size  <span class="comment"># TP rank 分片保存</span></span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    start, step = <span class="number">0</span>, <span class="number">1</span>  <span class="comment"># 完整保存，因为不同 TP rank 的 KV 不等价</span></span><br></pre></td></tr></table></figure><p>MLA 场景下不同 TP rank 分担保存任务，减少搬运量和存储冗余；非 MLA 场景按完整映射保存，保证后续复用的正确性。这样 DeepSeek 等 MLA 模型和 Qwen 等 GQA&#x2F;MHA 模型都能统一支持。</p><h3 id="进程间通信：为什么我们手写了基于-ZMQ-的-RPC"><a href="#进程间通信：为什么我们手写了基于-ZMQ-的-RPC" class="headerlink" title="进程间通信：为什么我们手写了基于 ZMQ 的 RPC"></a>进程间通信：为什么我们手写了基于 ZMQ 的 RPC</h3><p>实现跨 DP 域的 Prefix Cache 共享，block 查询和分配就必然需要跨进程通信。</p><p><code>vllm/distributed/device_communicators/shm_broadcast.py</code> 里有一个基于 ZMQ 封装的 MessageQueue 类，支持一对多的广播调用和返回值收集。但我们的场景是<strong>多对一</strong>（多个 worker&#x2F;scheduler 作为 Writer 调用 metadata 并收集返回值），直接复用会踩两个坑：</p><ol><li>需要开多个独立 MessageQueue，overhead 变大；</li><li>主进程拉起 EngineCore 子进程时，busy loop 的参数无法通过 <code>mp</code> 传递 MessageQueue 结构体（里面带有 socket 等不可序列化对象）。</li></ol><p>考虑了三种方案：</p><table><thead><tr><th>方案</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td>Pyro5 RPC 框架</td><td>代码少</td><td>新增依赖，性能不如 ZMQ</td></tr><tr><td>强行复用 MessageQueue</td><td>代码少</td><td>逻辑绕，每个 EngineCore 都要开独立 queue</td></tr><tr><td>手写基于 ZMQ 的通信</td><td>性能高，定制灵活</td><td>工作量稍大</td></tr></tbody></table><p>最终选了方案 3，复用了 <code>vllm.utils.make_zmq_socket</code> 来减少冗余。多对一 + 中心化 server 串行处理的模式恰好对应 <code>zmq.ROUTER</code> 和 <code>zmq.DEALER</code>，不需要手动在 server 侧实现 task 队列。</p><h3 id="共享内存的坑"><a href="#共享内存的坑" class="headerlink" title="共享内存的坑"></a>共享内存的坑</h3><p>最初想用 <code>pin_memory</code> + <code>share_memory_()</code> 来开辟共享内存：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">create_shared_pinned_tensor</span>():</span><br><span class="line">    tensor = torch.ones((<span class="number">3</span>, <span class="number">4</span>), pin_memory=<span class="literal">True</span>)</span><br><span class="line">    shared_tensor = tensor.share_memory_()</span><br><span class="line">    <span class="keyword">return</span> shared_tensor</span><br></pre></td></tr></table></figure><p>但实测发现通过 ZMQ + pickle 跨进程传输时，不同进程拿到的地址不同，怀疑每次都在新开辟空间。换用 <code>multiprocessing.SharedMemory</code> 后才正常工作：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing.shared_memory <span class="keyword">import</span> SharedMemory</span><br><span class="line"></span><br><span class="line">shm = SharedMemory(name=name, create=<span class="literal">True</span>, size=size)</span><br><span class="line">buf = torch.frombuffer(shm.buf, dtype=dtype)</span><br></pre></td></tr></table></figure><p>清理也要注意——Linux 下会在 <code>/dev/shm</code> 生成临时文件，必须在 client 端 <code>shm.close()</code> 后在 server 端 <code>shm.close()</code> + <code>shm.unlink()</code>。为了防止清理不干净或被残留文件影响，创建时加了保护：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">_safe_create_shared_memory</span>(<span class="params">name: <span class="built_in">str</span>, size: <span class="built_in">int</span></span>) -&gt; SharedMemory:</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        existing_shm = SharedMemory(name=name, create=<span class="literal">False</span>)</span><br><span class="line">        existing_shm.close()</span><br><span class="line">        existing_shm.unlink()</span><br><span class="line">    <span class="keyword">except</span> FileNotFoundError:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">return</span> SharedMemory(name=name, create=<span class="literal">True</span>, size=size)</span><br></pre></td></tr></table></figure><h3 id="一个隐蔽的-bug：profile-run-的显存估算"><a href="#一个隐蔽的-bug：profile-run-的显存估算" class="headerlink" title="一个隐蔽的 bug：profile_run() 的显存估算"></a>一个隐蔽的 bug：profile_run() 的显存估算</h3><p>开启 KVConnector 后在特定大并发场景下会稳定卡住或爆显存。排查发现 <code>profile_run()</code> 估算的剩余可用显存反而比不开时更大，导致 GPU KV Cache 多分配，最终爆显存但表现是卡住。修复是将逻辑中的 <code>is_kv_consumer</code> 改为 <code>not is_kv_producer</code>，让显存估算与实际 KV Cache 消费行为对齐。</p><h2 id="怎么测试：构造可控命中率的数据集"><a href="#怎么测试：构造可控命中率的数据集" class="headerlink" title="怎么测试：构造可控命中率的数据集"></a>怎么测试：构造可控命中率的数据集</h2><p>vLLM 的原生 benchmark 无法构造复杂命中率数据，所以我们自己设计了一套方法。</p><p>采用<strong>三段式数据格式</strong>：所有请求共用 common prefix + 同组请求共用 group prefix + 各自 random data。命中率统计最近 1k 条请求，KV Cache block 大小为 128。通过修改 <code>benchmark_serving.py</code> 增加三种重复发送模式来控制命中率：</p><ul><li><strong>sequential</strong>：将请求按顺序再发一遍，模拟连续访问相同数据。</li><li><strong>stack</strong>：倒序再发一遍，模拟反向访问。</li><li><strong>reverse</strong>：按奇偶拆分，结合 <code>num_group</code> 控制相同前缀的 req 每组两个，可以控制 GPU 开始驱逐时的命中率。</li></ul><p>举例来说：common prefix 1024、group prefix 1024、random data len 2048，1k 条请求每组 512 个——这时 GPU 命中率为 25%，但 HBM 容量不够导致 group prefix 被驱逐，而 DRAM 空间充足，每组的 group prefix 都能在 CPU 上命中，CPU 命中率 12.5%。调整这些参数和发送格式，可以构造多种命中率组合来测试。</p><h2 id="效果如何"><a href="#效果如何" class="headerlink" title="效果如何"></a>效果如何</h2><p>在 DeepSeek 上开启 CPU Offload 后基本没有性能损失，命中率越高收益越大。以 37.5% CPU 命中率为例，相比纯 GPU prefix-cache：</p><table><thead><tr><th>命中场景</th><th>吞吐收益</th></tr></thead><tbody><tr><td>纯 GPU 命中</td><td>-2.336%</td></tr><tr><td>37.5% CPU 命中</td><td>+22.64%</td></tr><tr><td>混合命中</td><td>+11.229%</td></tr></tbody></table><p>此外，在测试过程中通过 profiling 发现了一个值得关注的性能细节：GQA KV Cache 在 prefix 命中场景下，调用的算子性能较差，部分算子的执行时间明显长于预期。这提示在 GQA 模型上开启 Prefix Cache 时，除了缓存命中率本身，底层算子的实现效率也是影响端到端收益的关键因素，后续可以针对命中路径的算子做定向优化。</p><p>纯 GPU 命中场景下有一点点负收益（异步写入的开销），所以在 prefix 命中率不高的场景下不建议开启。但在输入存在大量语义重复的真实场景——Agent 对话、代码库分析、智能客服、文档批量处理——命中率通常很高，收益非常明显。</p><h2 id="交互流程"><a href="#交互流程" class="headerlink" title="交互流程"></a>交互流程</h2><p><img src="/images/CPUoffloading/%E4%BA%A4%E4%BA%92.png" alt="交互流程图"></p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>这个项目本质上做了一件事：把 CPU DRAM 当成 HBM 的二级缓存，在 vLLM V1 的 KVConnector 扩展点上实现了节点级的 Prefix Cache 共享。</p><p>回顾下来，几个比较关键的选择是：</p><ol><li><strong>控制面与数据面分离</strong>——Scheduler 管决策，Worker 管执行，MetadataServer 管全局状态，职责边界清晰，出问题也容易定位。</li><li><strong>两阶段缓存提交</strong>——先分配空间，等数据写完再标记可用。这个简单的设计避免了”命中到脏数据”的致命问题。</li><li><strong>逐层流水线加载</strong>——不等到所有层就绪再开始 forward，最大程度降低 swap-in 对 TTFT 的冲击。</li><li><strong>命中增益阈值</strong>——不是命中就搬，而是命中”赚得够多”才搬，避免小粒度搬运得不偿失。</li></ol><p>当然，如果 prefix 命中率本身很低，CPU Offloading 的异步写入开销就是纯负收益，不建议无脑开启。它最适合的场景是输入中有大量稳定、可复用的语义前缀——而这恰好是 Agent 和 RAG 时代越来越普遍的负载特征。</p><h2 id="笔者贡献"><a href="#笔者贡献" class="headerlink" title="笔者贡献"></a>笔者贡献</h2><p>我有幸深度参与了 vLLM-Ascend CPU Offloading 特性的完整研发周期，主要贡献如下：</p><p><strong>方案设计与讨论</strong>：参与了 CPU Offloading 整体方案的技术讨论和评审，包括节点级 Prefix Cache 的共享边界设计、CPU&#x2F;NPU 数据通路的异步流水线方案、命中增益阈值的策略建模，以及与 MetadataServer 的接口协议定义。在方案迭代过程中推动了两阶段缓存提交机制和逐层 swap-in 流水线的落地，前者从设计上杜绝了”命中脏数据”的一致性问题，后者有效控制了 swap-in 对 TTFT 的延迟冲击。</p><p><strong>收益测试与性能分析</strong>：独立设计并实现了三段式数据集构造方法和 benchmark 重复发送策略（sequential &#x2F; stack &#x2F; reverse），能够灵活构造 0% 到 100% 之间多种 GPU&#x2F;CPU 命中率组合，填补了 vLLM 原生 benchmark 在复杂命中场景下的测试空白。基于该工具完成了全场景性能收益测试，在 DeepSeek 模型上系统性验证了 CPU Offloading 的有效性边界。此外，通过 profiling 深入分析发现 GQA KV Cache 在 prefix 命中场景下存在算子性能瓶颈，为后续性能优化指明了方向。以上工作为方案的合并决策和适用场景推荐提供了关键数据支撑。</p><p><strong>稳定性修复</strong>：排查并修复了 <code>profile_run()</code> 显存估算偏差的隐蔽 bug——该问题在特定大并发场景下导致显存超分、进程卡死，定位难度较大。根因是 <code>is_kv_consumer</code> 判断逻辑导致 KVConnector 场景下的剩余显存估算偏高，修复为 <code>not is_kv_producer</code> 后显存估算与实际的 KV Cache 分配行为对齐，消除了此场景下的稳定性隐患。</p><blockquote><p>注：随着 vLLM 主仓在后续版本中原生支持了 CPU Offloading，vLLM-Ascend 中的 CPU Offloading 插件已从代码库中移除，相关能力由主仓统一提供。本文记录的方案设计、工程踩坑和性能分析经验，对理解 vLLM KVConnector 扩展机制和异构缓存系统设计仍有一定参考价值。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 推理优化 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> vLLM </tag>
            
            <tag> Ascend </tag>
            
            <tag> CPU Offloading </tag>
            
            <tag> Prefix Cache </tag>
            
            <tag> KV Cache </tag>
            
            <tag> 推理优化 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
